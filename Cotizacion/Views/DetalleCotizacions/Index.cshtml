@model IEnumerable<Cotizacion.Models.DetalleCotizacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cotizacion.CotizacionId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Producto.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cotizacion.CotizacionId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DetalleCotizacionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DetalleCotizacionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DetalleCotizacionId })
        </td>
    </tr>
}

</table>
